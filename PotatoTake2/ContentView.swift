//
//  ContentView.swift
//  PotatoTake2
//
//  Created by Jacen H. on 5/9/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("J.H. - French fries")
            Text("Something that doesn't crash it")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
